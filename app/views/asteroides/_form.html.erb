<%= form_with(model: asteroide, local: true) do |form| %>
  <% if asteroide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asteroide.errors.count, "error") %> prohibited this asteroide from being saved:</h2>

      <ul>
      <% asteroide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :asteroide_nombre %>
  </div>

  <div class="field">
    <%= form.label :Advertencia %>
    <%= form.text_area :Advertencia, id: :asteroide_Advertencia %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
